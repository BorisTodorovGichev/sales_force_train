set<Id> myIds = new set<id>();
System.debug('****** IDs' + myIds);
myIds.add('0011t000003ie62');
System.debug('****** IDs' + myIds);

list<Account> accounts = [select id, Website from Account where id IN :myIds];
System.debug('****** IDs' + accounts);

for (Account acc : accounts) {
    acc.Website = 'newWebsite';
    System.debug('***' + acc.Website);
}

update accounts;